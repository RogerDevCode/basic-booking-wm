repos:
  - repo: local
    hooks:
      - id: gen-ai-summary
        name: "Regenerate .ai-codex/summary.md"
        entry: python scripts/gen_summary.py
        language: python
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
