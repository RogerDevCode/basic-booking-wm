# Salida esperada por cada línea: una guía .md con conceptos, patrones, ejemplos y errores comunes.

mejores prácticas para el manejo de PostgreSQL 17 con Go (lib/pq) y Windmill: connection pooling, pool sizing, transacciones, manejo de errores, JSONB, índices y esquemas en un contexto de booking system
mejores prácticas para el manejo de Redis con Go y Windmill: uso como distributed lock, cache de disponibilidad, sesiones, persistencia AOF, TTL y patrones Lua scripting en un booking system
mejores prácticas para escribir scripts Go en Windmill: estructura de un script, manejo de Resource Types (RT.Postgresql, RT.Redis), variables de entorno, logging, retries y testing local con wmill CLI
mejores prácticas para diseñar Flows en Windmill: composición de pasos, manejo de errores por step, branching condicional, suspend/resume, idempotencia y orquestación de booking workflows en YAML
mejores prácticas para integrar Telegram Bot API con Windmill y Go: recepción de webhooks, validación de tokens, parseo de mensajes, respuestas con MarkdownV2, rate limiting (30 msg/s) y flujo de conversación en un AI booking agent
mejores prácticas para integrar Gmail API con Go usando OAuth2 Service Account: envío transaccional, manejo de App Passwords, plantillas de email, reintentos con backoff y monitoreo de bounces en un sistema de notificaciones de booking
mejores prácticas para integrar Google Calendar API con Go usando Service Account: creación y eliminación de eventos, manejo de timezones (UTC), concurrencia, rollback de eventos y sincronización con un booking system
mejores prácticas para implementar el patrón Circuit Breaker en Go con PostgreSQL: estados (closed/open/half-open), persistencia de estado en DB, umbrales, ventanas de tiempo y uso dentro de scripts Windmill
mejores prácticas para implementar Distributed Locks en Go con Redis y PostgreSQL: algoritmo Redlock vs advisory locks de Postgres, TTL, auto-release, prevención de double-booking y manejo de expiración en Windmill scripts
mejores prácticas para implementar Dead Letter Queue (DLQ) en Go con PostgreSQL: esquema de tabla, reintentos con backoff exponencial, alertas, replay de mensajes fallidos y monitoreo en un sistema de booking
mejores prácticas para implementar idempotencia en un booking system con Go y PostgreSQL: generación de idempotency keys (SHA256), almacenamiento, ventanas de deduplicación y uso en scripts Windmill
mejores prácticas para construir un AI Agent con Groq (Llama 3.3 70B) y OpenAI como fallback en Go con TypeScript/Bun en Windmill: detección de intenciones, manejo de contexto conversacional, fallback strategy y parseo de respuestas JSON para accionar bookings
mejores prácticas para estructurar un API Gateway HTTP en Go: routing, middlewares, timeouts, graceful shutdown, health checks, validación de request/response y documentación de endpoints en un proyecto con Windmill como backend de automatización
mejores prácticas para configurar Nginx como reverse proxy en Docker para una API Go: rate limiting por IP (10r/s), burst, gzip, keepalive, security headers, TLS 1.3 y protección contra ataques comunes
mejores prácticas para usar Cloudflare Tunnel (cloudflared) en un stack Docker con Go y Windmill: configuración del tunnel, exposición segura sin abrir puertos, manejo del token, healthchecks y troubleshooting de conectividad
mejores prácticas para dockerizar una aplicación Go con multi-stage build: builder stage (Go alpine), runner stage (alpine mínimo), usuario non-root, healthcheck HTTP, variables de entorno y composición con docker-compose para producción (8 servicios)
mejores prácticas para el manejo de Resource Types y variables de entorno secretas en Windmill: diferencia entre resources y variables, uso de RT.Postgresql / RT.Gcal / RT.Telegram desde scripts Go y TypeScript, rotación de secrets y auditoría
mejores prácticas para escribir scripts TypeScript con Bun en Windmill: parseo de mensajes Telegram, estructura de módulos, interacción con recursos, tipado estricto, manejo de errores y pruebas locales con bun test
mejores prácticas para implementar un sistema de rollback transaccional en Go para un booking orchestrator: coordinación multi-paso (DB + GCal + Redis), orden de compensación, logging de fallos y reintegración con DLQ en Windmill flows
mejores prácticas para testing en Go de un booking system con Windmill: tests unitarios con stdlib testing, mocks de dependencias externas (DB, APIs), tests de integración con Docker, cobertura HTML y estrategia de CI/CD con GitHub Actions
