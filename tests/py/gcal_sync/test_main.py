import pytest

import f.gcal_sync.main


# This is an auto-generated test boilerplate for f.gcal_sync.main
@pytest.mark.asyncio
async def test_main_basic_import() -> None:
    # Ensure the module is importable and has basic structure
    assert f.gcal_sync.main is not None
