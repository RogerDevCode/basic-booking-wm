import pytest

import f.booking_orchestrator.handlers._cancel


# This is an auto-generated test boilerplate for f.booking_orchestrator.handlers._cancel
@pytest.mark.asyncio
async def test__cancel_basic_import() -> None:
    # Ensure the module is importable and has basic structure
    assert f.booking_orchestrator.handlers._cancel is not None
