# ============================================================================
# .env.test.example — Template para pruebas locales
# ============================================================================
# Copia este archivo como .env.test y completa tus credenciales locales.
# El archivo .env.test está en .gitignore — NUNCA lo commitees.
# ============================================================================

export DATABASE_URL='postgresql://booking:CHANGE_ME@localhost:5433/bookings'
